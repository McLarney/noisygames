use ndarray::arr2;

struct Game {
    a_mtx: arr2,
    b_mtx: arr2,
}
fn buildgame(arr2: a_payoff, arr2: b_payoff) {
    //there are a few things we'd like to check before storing our payoff matrices into the game
    //object.
    //Firstly, we'd like to make sure that the dimensionality of the matrices is the same. A game
    //doesn't make sense otherwise
    //We'd also like to check that the entries are all numeric.
    
    println!()
}
